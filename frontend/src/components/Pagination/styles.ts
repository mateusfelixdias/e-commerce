import styled from 'styled-components';

export const Container = styled.div`
  gap: 0.5rem;
  display: flex;
  margin-top: 2rem;
  align-items: center;
`;

export const ButtonsContainer = styled.div`
  gap: 0.125rem;
  display: flex;
`;

export const PoteDots = styled.span`
  padding-top: 1rem;
  color: var(--dark-grey);
`;
